<UserControl x:Class="WPF_TravelApp.Views.AddTripView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_TravelApp.Views"
             xmlns:vm="clr-namespace:WPF_TravelApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:AddTripViewModel}">
    <StackPanel>
        <Label FontSize="25" Content="Trip Name"/>
        <TextBox FontSize="20" Text="{Binding Trip.TripName}"/>
        <Label FontSize="25" Content="Destination"/>
        <TextBox FontSize="20" Text="{Binding Trip.Destination}"/>
        <Label FontSize="25" Content="Departure"/>
        <TextBox FontSize="20" Text="{Binding Trip.Departure}"/>
        <Label FontSize="25" Content="Arrival"/>
        <TextBox FontSize="20" Text="{Binding Trip.Arrival}"/>
        <UniformGrid Columns="2">
            <Button FontSize="25" Content="Cancel" Command="{Binding CancelAddTripCommand}"/>
            <Button FontSize="25" Content="Add" Command="{Binding AddTripCommand}"/>
        </UniformGrid>
    </StackPanel>
</UserControl>
