<UserControl x:Class="WPF_TravelApp.Views.TripAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_TravelApp.Views"
             xmlns:vm="clr-namespace:WPF_TravelApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance vm:TripAddViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <ComboBox
            SelectedIndex="0" 
            FontSize="20" 
            ItemsSource="{Binding Trips}" 
            DisplayMemberPath="TripName"/>
        <Button Grid.Row="1" Content="Go to Home page" Command="{Binding GoHomePageCommand}"/>
        <UniformGrid Grid.Row="2">
            <Button Content="Cancel"/>
            <Button Content="AddTrip" Command="{Binding AddTripCommand}"/>
        </UniformGrid>
    </Grid>
</UserControl>
