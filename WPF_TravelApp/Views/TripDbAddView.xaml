<UserControl x:Class="WPF_TravelApp.Views.TripDbAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_TravelApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <StackPanel>
        <Label Content="Trip Name"/>
        <TextBox Text="{Binding Trip.TripName}"/>
        <Label Content="Destination"/>
        <TextBox Text="{Binding Trip.Destination}"/>
        <Label Content="Departure"/>
        <TextBox Text="{Binding Trip.Departure}"/>
        <Label Content="Arrival"/>
        <TextBox Text="{Binding Trip.Arrival}"/>
        <Button FontSize="20" Content="Add Trip in db" Command="{Binding AddTripDbCommand}"/>
    </StackPanel>
</UserControl>
