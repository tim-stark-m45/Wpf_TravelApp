<UserControl x:Class="WPF_TravelApp.Views.TripObserveUCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_TravelApp.Views"
             xmlns:vm="clr-namespace:WPF_TravelApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button FontSize="25" Content="1" Command="{Binding TestWindowCommand}" CommandParameter="1"/>
            <Button FontSize="25" Content="2" Command="{Binding TestWindowCommand}" CommandParameter="2"/>
            <Button FontSize="25" Content="3" Command="{Binding TestWindowCommand}" CommandParameter="3"/>
        </StackPanel>
    </Grid>
</UserControl>
